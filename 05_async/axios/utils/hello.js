export const hello = (message) => {
  return message;
};

/*
  // installation des dependances en production
  npm install express --save
  npm install express
  npm i express

  // installation des dependances en dev
  npm install nodemon --save-dev
  npm install nodemon --dev
  npm i nodemon --dev

*/
